<resources>

    <!-- Base application theme. -->
    <style name="LightTheme" parent="android:Theme.Material.Light.DarkActionBar">
        <item name="android:windowBackground">@drawable/backgroundlight</item>
    </style>

    <style name="DarkTheme" parent="android:Theme.Material.Light.DarkActionBar">
        <item name="android:windowBackground">@drawable/backgrounddark</item>
    </style>

</resources>
